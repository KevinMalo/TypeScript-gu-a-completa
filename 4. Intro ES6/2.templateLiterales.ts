let brand = "Tesla";
let model = 'X';
let color = `black`;

function congrats() {
    return "Gracias por su compra";
}

let mensaje = `Hola, su auto es un ${brand} de modelo ${model} y es de color ${color}.
${congrats()}`;

console.log( mensaje );
